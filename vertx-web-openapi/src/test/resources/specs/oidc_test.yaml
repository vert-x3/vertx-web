openapi: 3.0.0

info:
  description: This service provides APIs to eat chocolate.
  version: 0.0.1
  title: chocolate Service

tags:
  - name: chocolates

components:
  securitySchemes:
    openId:
      type: openIdConnect
      openIdConnectUrl: http://example.com/auth/realms/master/.well-known/openid-configuration

security:
  - oauth2:
      - scopeA
paths:
  /candy:
    post:
      operationId: candy
      tags:
        - chocolates
      responses:
        200:
          description: Something
